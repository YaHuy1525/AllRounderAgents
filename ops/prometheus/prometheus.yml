# Prometheus scrape config for the compose ops stack.
# The API exposes process-local metrics at /metrics (no cross-worker aggregation).
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: allrounder-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
